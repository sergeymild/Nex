plugins {
    id("maven")
    id("java-library")
}


sourceCompatibility = "8"
targetCompatibility = "8"

group = "com.nex"
version = "1.0"

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: uri('../repo'))
        }
    }
}